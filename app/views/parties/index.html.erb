<h1>It's Party Time!</h1>

<% if @parties.size > 0 %>
  <ul>
  <% @parties.each do |party| %>
    <li><%= link_to party.name, party_path(party) %></li>
  <% end %>
  </ul>
<% end %>

<p><%= link_to "New Party", new_party_path %>